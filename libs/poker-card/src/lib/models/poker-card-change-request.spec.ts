import { PokerCardChangeRequest } from './poker-card-change-request';

describe('PokerCardChangeRequest', () => {
  it('should create an instance', () => {
    expect(new PokerCardChangeRequest()).toBeTruthy();
  });
});
